services:
  multilingual-bookstore:
    build:
      context: .
      image: multilingual-bookstore:latest
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./multilingual-bookstore.db:/src/multilingual-bookstore.db
    ports:
      - "80:8000"
    expose:
      - 8000